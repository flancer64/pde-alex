# Web server configuration loaded from the application root.
TEQFW_WEB__HOST=127.0.0.1
TEQFW_WEB__PORT=3000
TEQFW_WEB__TYPE=http

# Personal Digital Embassy Runtime configuration.
PDE__DATA_ROOT=var
PDE_RUNTIME__BASE_URL=http://127.0.0.1:3000
PDE_RUNTIME__COOKIE_SECURE=false
# Generate before use: node -e "console.log(require('node:crypto').randomBytes(48).toString('base64url'))"
# Never commit or log the generated Owner secret.
PDE_RUNTIME__OWNER_SECRET=
PDE_RUNTIME__OWNER_SESSION_TTL_SECONDS=3600
PDE_RUNTIME__AUTHORIZATION_REQUEST_TTL_SECONDS=300
PDE_RUNTIME__AUTHORIZATION_CODE_TTL_SECONDS=120
PDE_RUNTIME__ACCESS_TOKEN_TTL_SECONDS=900
# Trusted Person magic-link delivery through SMTP.
# MODE selects the SMTP transport: implicit-tls (TLS from connect), starttls (SMTP upgrade), or plain (no TLS).
# Default example: implicit TLS on port 465.
# Leave both USER and PASSWORD empty only when the SMTP server allows unauthenticated relay.
# When authentication is required, set both values together.
PDE_RUNTIME__TRUSTED_PERSON_SMTP_HOST=smtp.example.com
PDE_RUNTIME__TRUSTED_PERSON_SMTP_PORT=465
PDE_RUNTIME__TRUSTED_PERSON_SMTP_MODE=implicit-tls
PDE_RUNTIME__TRUSTED_PERSON_SMTP_SECURE=true
PDE_RUNTIME__TRUSTED_PERSON_SMTP_USER=
PDE_RUNTIME__TRUSTED_PERSON_SMTP_PASSWORD=
PDE_RUNTIME__TRUSTED_PERSON_SMTP_FROM=no-reply@example.com
# STARTTLS example for SMTP submission on port 587:
# PDE_RUNTIME__TRUSTED_PERSON_SMTP_PORT=587
# PDE_RUNTIME__TRUSTED_PERSON_SMTP_MODE=starttls
# PDE_RUNTIME__TRUSTED_PERSON_SMTP_SECURE=false
# Plain SMTP example for a trusted local network only:
# PDE_RUNTIME__TRUSTED_PERSON_SMTP_PORT=25
# PDE_RUNTIME__TRUSTED_PERSON_SMTP_MODE=plain
# PDE_RUNTIME__TRUSTED_PERSON_SMTP_SECURE=false
# Optional explicit trusted Client metadata records for controlled/local integrations.
# PDE_RUNTIME__TRUSTED_CLIENTS_JSON={}

# Initial durable-state database. Production uses an absolute protected path.
TEQFW_DB__CLIENT=sqlite3
TEQFW_DB__FILENAME=var/state.sqlite
TEQFW_DB__USE_NULL_AS_DEFAULT=true

# Telegram Desk configuration. Never commit TDLib state or live credentials.
PDE_DESK_TELEGRAM__API_ID=
PDE_DESK_TELEGRAM__API_HASH=
PDE_DESK_TELEGRAM__TDJSON_PATH=
PDE_DESK_TELEGRAM__DATABASE_DIRECTORY=./var/telegram/test-dc/database
PDE_DESK_TELEGRAM__FILES_DIRECTORY=./var/telegram/test-dc/files
PDE_DESK_TELEGRAM__USE_TEST_DC=true
PDE_DESK_TELEGRAM__SEND_TIMEOUT_MS=30000

# Files Desk configuration.
PDE_DESK_FILES__ROOT=/srv/embassy/shared
PDE_DESK_FILES__MAX_FILE_BYTES=1048576
PDE_DESK_FILES__MAX_DIRECTORY_ENTRIES=1000
